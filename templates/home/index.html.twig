{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}


    <div class="example-wrapper">

        {% if app.user %}
            {% if is_granted('ROLE_USER') %}
                <div class="mb-3">
                    Bonjour {{ app.user.username }} ravi de te revoir, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}
        {% endif %}

    </div>
{% endblock %}
